version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto17

  build:
    commands:
      - chmod +x mvnw
      - ./mvnw clean package

  post_build:
    commands:
      - cp target/order-management-0.0.1-SNAPSHOT.jar application.jar

artifacts:
  files:
    - application.jar